# Local development (default if unset)
VITE_API_URL=http://127.0.0.1:8000/api

# Production example (set this in Netlify → Environment variables, then redeploy):
# VITE_API_URL=https://your-django-api.example.com/api
